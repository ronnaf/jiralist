import styled from 'styled-components';
import { colors } from './Styles';

export const JRawDiv = styled.div`
  padding: 16px;
  border-radius: 4px;
  background-color: ${colors.background2};
  box-shadow: 5px 5px 0px 0px ${colors.shadow};
`;
